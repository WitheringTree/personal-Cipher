import time
import random
class First:
    comprehensive={}
    first=[]
    step=[]
    lock=""
    mess=""
    folder=""

    #lock=input("input KEY: ")

    #This is a test string, should be removed after testing. Or rendered inert
    lock="45plfao7vansepm7eifnpae4anva"

    step=list(lock)
    folder=step.pop

    positions=0
    
    comprehensive=[[] for i in range(positions)]



    mess=input("Place your message here: ")

    for char in mess:
            first.append(char)
    if first != lock:
         #Dud classification
        print("7")
    # 1
    # 2
    # 3



    time.sleep(100)


'''
    question0=""    
    answer1=0

    while answer1==0:
        question0=input("Encode or Decode? ")
        if "1" == question0:
            #encode
            answer1=1  
        elif "2" == question0:
            #decode
            answer1=2
        else:
            print("Not a valid answer")    

            

    length_Number_Characters=[]
    wheel=[]
    length_Bite_Size=[]    
    distributor=[]
    intermeditary=[]
    
    while count_Down>0:
    distributor=lock.pop()
    intermeditary=len(distributor)
    length_Number_Characters.append(intermeditary)
    wheel.append(distributor)
    for char in distributor:
        length_Bite_Size.append(char)
'''